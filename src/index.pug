mixin imgWithSize(src)
  - const { width, height } = imageSize(src);
  img(src=src alt="" width=width height=height)

doctype html
html
  head
    meta(charset='utf-8')
    title img width, height自動付与
  body
    +imgWithSize('150x150.png')
    +imgWithSize('300x200.png')
